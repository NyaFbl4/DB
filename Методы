Сигнатура метода

private void ShowText(string newText)
{
    Text.text = newText;
}

метод является приватным (вызывается только внутри своего класса), не возвращает ничего, на вход запрашивает данные типа string

